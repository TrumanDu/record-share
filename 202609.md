

### Tencent/teamai-cli
https://github.com/Tencent/teamai-cli


TeamAI CLI 是腾讯开源的一款团队协同 AI 工具，通过 Git 仓库集中管理团队的 AI 技能、规则、知识和代理配置。它支持多种主流 AI 代理（如 Claude Code、Codex、Cursor），实现“一键同步”团队资源：成员通过 `npm install -g teamai-cli` 安装后，运行 `teamai init` 初始化共享仓库，即可自动同步技能规则。核心结构分为三层：**团队执行层**管理技能、规则和钩子；**团队上下文层（beta）** 实现知识检索（`teamai recall`）和代码知识图谱（`teamai codebase`）；**团队改进层（beta）** 提供会话分析、维护工具（`teamai digest`）和优化建议。支持 GitHub/GitLab 等平台，通过 MR 流程审核更新，增强团队协作效率和 AI 工具标准化。

